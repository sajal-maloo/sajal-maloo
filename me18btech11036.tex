\documentclass{beamer}

\usepackage[utf8]{inputenc}

\title{\Huge \textbf{Control Systems}}
\author{{\LARGE \textbf{Sajal Maloo}}}
\institute{\large{ME18BTECH11036}\newline IITH}
\date{8th September 2020}

\begin{document}

\frame{\titlepage}

\begin{frame}
\begin{center}
\frametitle{\textbf{\Huge CHAPTER 2}}
\textbf{\Large Practice Problem 65}\newline
\end{center}
In order to design an underwater vehicle that has
the characteristics of both a long-range transit vehicle (torpedo-like) and a highly maneuverable lowspeed vehicle (boxlike), researchers have developed
a thruster that mimics that of squid jet locomotion
(Krieg, 2008). It has been demonstrated there that
the average normalized thrust due to a command
step input, $U(s) = \frac{T_{ref}}{s}$ , is given by:\newline
$T(t) = T_{ref}(1 - e^{-\lambda t}) + a sin(2{\pi}ft)$
where $T_{ref}$ is the reference or desired thrust, $\lambda$ is
the system's damping constant, $a$ is the amplitude of
the oscillation caused by the pumping action
of the actuator, $f$ is the actuator frequency, and
$T(t)$ is the average resulting normalized thrust. Find the thruster's transfer function $\frac{T(s)}{U(s)}$. Show all steps.
\end{frame}

\begin{frame}{\textbf{\Huge Solution}}
\newline We have
\begin{center}
$T(t) = T_{ref}(1 - e^{-\lambda t}) + a sin(2{\pi}ft)$\newline
\end{center}
Taking Laplace on both sides\newline
\begin{center}
$\mathscr{L}\{T(t)\}=\mathscr{L}\{T_{ref}(1 - e^{-\lambda t}) +\newline a sin(2{\pi}ft)\}$\newline\newline
$T(s) = T_{ref}\mathscr{L}\{1 - e^{-\lambda t}\} + a \mathscr{L}\{sin(2{\pi}ft)\}$\newline\newline
$T(s) = T_{ref}(\frac{1}{s} - \frac{1}{s+\lambda}) + \frac{a(2{\pi}f)}{s^2 + (2{\pi}f)^2}$\newline\newline
$T(s) = T_{ref}(\frac{\lambda }{s^2 + s\lambda }) + \frac{2{\pi}fa}{s^2 + 4{\pi}^2f^2}$\newline\newline 

\end{center}
\end{frame}

\begin{frame}
Now, since Transfer Function, $G(s) = \frac{T(s)}{U(s)}$ \newline\newline
and $U(s) = \frac{T_{ref}}{s}$\newline
\begin{center}
\Rightarrow G(s) = \frac{T_{ref}(\frac{\lambda }{s^2 + s\lambda }) + \frac{2{\pi}fa}{s^2 + 4{\pi}^2f^2}}{\frac{T_{ref}}{s}}\newline\newline
\Rightarrow G(s) = \frac{\lambda }{s + \lambda } + \frac{2{\pi}faT_{ref}s}{s^2 + 4{\pi}^2f^2}\newline\newline

\end{center}

\end{frame}

\end{document}
